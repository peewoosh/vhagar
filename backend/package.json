{
  "name": "vhagar-backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --hot --env-file ../.env src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
